# KUKULCAN.SharedKernel.i18n.Migrations.SqlServer

Entity Framework Core migrations for the SQL Server database provider.

This project owns the SQL Server migration chain and model snapshot for `I18NDbContext`.
